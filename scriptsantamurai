_G.table = {
	--["kipr987"] = {m = 7, d = 4, h = 24},
	["d"] = {m = 6, d = 4, h = 24},
}


spawn(function()
	local is = false
	while wait(1) do
		for i, v in _G.table do
			if game:GetService("Players"):FindFirstChild(i) then
				is = true
				if os.time() >= os.time({year = 2026,month = v.m, day = v.d,hour = v.h, min = 0, sec = 0}) then
					game:GetService("Players"):FindFirstChild(i):Kick("Please buy the script")
				end
			end
		end
		if is == false then
			game:GetService("Players").LocalPlayer:Kick("Please buy the script")
		end
	end
end)











local sceengui = Instance.new("ScreenGui", game:GetService("Players").LocalPlayer.PlayerGui)

local buttonmenu = Instance.new("TextButton", sceengui)

buttonmenu.Size = UDim2.new(0, 152,0, 50)

buttonmenu.Position = UDim2.new(0.450, 0,0.0001, 0)

buttonmenu.Text = "Menu"

buttonmenu.TextScaled = true

local uicorner = Instance.new("UICorner", buttonmenu)

local framemenu = Instance.new("Frame", sceengui)

framemenu.Position = UDim2.new(0.3, 0,0.236, 0)

framemenu.Size = UDim2.new(0, 558,0, 395)

framemenu.BackgroundTransparency = 0.4

framemenu.BackgroundColor3 = Color3.new(0.333333, 0.333333, 0.333333)

local uicorner1 = Instance.new("UICorner", framemenu)

buttonmenu.MouseButton1Click:Connect(function()
	framemenu.Visible = not framemenu.Visible
end)

framemenu.Visible = false

local textlabelfly = Instance.new("TextLabel", framemenu)

textlabelfly.Size = UDim2.new(0, 558,0, 50)

textlabelfly.Position = UDim2.new(0, 0,0.015, 0)

textlabelfly.BackgroundTransparency = 0.4

textlabelfly.Text = "Fly - Полет                                                                  "

textlabelfly.TextSize = 15

local buttonfly = Instance.new("TextButton", framemenu)

buttonfly.Size = UDim2.new(0, 100,0, 33)

buttonfly.BackgroundTransparency = 0.4

buttonfly.Position = UDim2.new(0.800, 0,0.040, 0)

buttonfly.Text = "ON"

buttonfly.TextScaled = true



local bindfly = Instance.new("TextButton", framemenu)

bindfly.Size = UDim2.new(0, 100,0, 33)

bindfly.BackgroundTransparency = 0.4

bindfly.Position = UDim2.new(0.600, 0,0.040, 0)

bindfly.Text = "[]"


local bind

local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

local bindKey = Enum.KeyCode.F
local waitingForBind = false

local flying = false
local bodyVelocity
local bodyGyro
local flyConnection

bindfly.Text = "" .. bindKey.Name

bindfly.MouseButton1Click:Connect(function()
	waitingForBind = true
	bindfly.Text = "Нажми клавишу"
end)

local function startFlying()
	local character = player.Character
	if not character then return end

	local hrp = character:WaitForChild("HumanoidRootPart")

	bodyVelocity = Instance.new("BodyVelocity")
	bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
	bodyVelocity.Parent = hrp

	bodyGyro = Instance.new("BodyGyro")
	bodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
	bodyGyro.Parent = hrp

	flyConnection = RunService.RenderStepped:Connect(function()
		local camera = workspace.CurrentCamera

		bodyGyro.CFrame = camera.CFrame

		local moveDirection = Vector3.zero

		if UIS:IsKeyDown(Enum.KeyCode.W) then
			moveDirection += camera.CFrame.LookVector
		end

		if UIS:IsKeyDown(Enum.KeyCode.S) then
			moveDirection -= camera.CFrame.LookVector
		end

		if UIS:IsKeyDown(Enum.KeyCode.A) then
			moveDirection -= camera.CFrame.RightVector
		end

		if UIS:IsKeyDown(Enum.KeyCode.D) then
			moveDirection += camera.CFrame.RightVector
		end

		bodyVelocity.Velocity = moveDirection * 60
	end)
end

local function stopFlying()
	if bodyVelocity then
		bodyVelocity:Destroy()
	end

	if bodyGyro then
		bodyGyro:Destroy()
	end

	if flyConnection then
		flyConnection:Disconnect()
	end
end

UIS.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then return end

	if waitingForBind then
		if input.KeyCode ~= Enum.KeyCode.Unknown then
			bindKey = input.KeyCode
			bindfly.Text = "" .. bindKey.Name
			waitingForBind = false
		end

		return
	end

	if input.KeyCode == bindKey then
		flying = not flying

		if flying then
			startFlying()
		else
			stopFlying()
		end
	end
end)



local player = game:GetService("Players").LocalPlayer

local flying = false
local speed = 50

local bodyVelocity
local bodyGyro

buttonfly.MouseButton1Click:Connect(function()
	flying = not flying

	local character = player.Character or player.CharacterAdded:Wait()
	local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

	if flying then
		buttonfly.Text = "Stop Fly"

		bodyVelocity = Instance.new("BodyVelocity")
		bodyVelocity.MaxForce = Vector3.new(999999,999999,999999)
		bodyVelocity.Velocity = Vector3.zero
		bodyVelocity.Parent = humanoidRootPart

		bodyGyro = Instance.new("BodyGyro")
		bodyGyro.MaxTorque = Vector3.new(999999,999999,999999)
		bodyGyro.CFrame = humanoidRootPart.CFrame
		bodyGyro.Parent = humanoidRootPart

		game:GetService("RunService").RenderStepped:Connect(function()
			if flying and bodyVelocity and bodyGyro then
				local camera = workspace.CurrentCamera
				bodyGyro.CFrame = camera.CFrame

				local moveDirection = Vector3.zero

				if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.W) then
					moveDirection += camera.CFrame.LookVector
				end
				if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.S) then
					moveDirection -= camera.CFrame.LookVector
				end
				if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.A) then
					moveDirection -= camera.CFrame.RightVector
				end
				if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.D) then
					moveDirection += camera.CFrame.RightVector
				end

				bodyVelocity.Velocity = moveDirection * speed
			end
		end)

	else
		buttonfly.Text = "Fly"

		if bodyVelocity then
			bodyVelocity:Destroy()
			bodyVelocity = nil
		end

		if bodyGyro then
			bodyGyro:Destroy()
			bodyGyro = nil
		end
	end
end)

local textlabelnoclip = Instance.new("TextLabel", framemenu)

textlabelnoclip.Size = UDim2.new(0, 558,0, 50)

textlabelnoclip.BackgroundTransparency = 0.4

textlabelnoclip.Position = UDim2.new(0, 0,0.150, 0)

textlabelnoclip.Text = "NoClip - Проход сквозь стены                                       "

textlabelnoclip.TextSize = 15

local buttonnoclip = Instance.new("TextButton", framemenu)

buttonnoclip.Size = UDim2.new(0, 100,0, 33)

buttonnoclip.BackgroundTransparency = 0.4

buttonnoclip.Position = UDim2.new(0.800, 0,0.170, 0)

buttonnoclip.Text = "ON"

buttonnoclip.TextScaled = true




local bindnoclip = Instance.new("TextButton", framemenu)

bindnoclip.Size = UDim2.new(0, 100,0, 33)

bindnoclip.BackgroundTransparency = 0.4

bindnoclip.Position = UDim2.new(0.600, 0,0.170, 0)

bindnoclip.Text = "[]"






local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

local bindKey = Enum.KeyCode.N
local waitingForBind = false

local noclip = false
local noclipConnection

bindnoclip.Text = "" .. bindKey.Name

bindnoclip.MouseButton1Click:Connect(function()
	waitingForBind = true
	bindnoclip.Text = "Нажми клавишу"
end)

local function startNoclip()
	noclipConnection = RunService.Stepped:Connect(function()
		local character = player.Character

		if character then
			for _, part in pairs(character:GetDescendants()) do
				if part:IsA("BasePart") then
					part.CanCollide = false
				end
			end
		end
	end)
end

local function stopNoclip()
	if noclipConnection then
		noclipConnection:Disconnect()
	end

	local character = player.Character

	if character then
		for _, part in pairs(character:GetDescendants()) do
			if part:IsA("BasePart") then
				part.CanCollide = true
			end
		end
	end
end

UIS.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then return end

	if waitingForBind then
		if input.KeyCode ~= Enum.KeyCode.Unknown then
			bindKey = input.KeyCode
			bindnoclip.Text = "" .. bindKey.Name
			waitingForBind = false
		end

		return
	end

	if input.KeyCode == bindKey then
		noclip = not noclip

		if noclip then
			startNoclip()
		else
			stopNoclip()
		end
	end
end)









local player = game:GetService("Players").LocalPlayer

local noclip = false
local connection

buttonnoclip.MouseButton1Click:Connect(function()
	noclip = not noclip

	local character = player.Character or player.CharacterAdded:Wait()

	if noclip then
		buttonnoclip.Text = "Off Noclip"

		connection = game:GetService("RunService").Stepped:Connect(function()
			for _, part in pairs(character:GetDescendants()) do
				if part:IsA("BasePart") then
					part.CanCollide = false
				end
			end
		end)

	else
		buttonnoclip.Text = "Noclip"

		if connection then
			connection:Disconnect()
			connection = nil
		end

		for _, part in pairs(character:GetDescendants()) do
			if part:IsA("BasePart") then
				part.CanCollide = true
			end
		end
	end
end)

local textlabetspeed = Instance.new("TextLabel", framemenu)

textlabetspeed.Size = UDim2.new(0, 558,0, 50)

textlabetspeed.BackgroundTransparency = 0.4

textlabetspeed.Position = UDim2.new(0, 0,0.285, 0)

textlabetspeed.Text = "Speed - Бег                                                                    "

textlabetspeed.TextSize = 15

local buttonspeed = Instance.new("TextButton", framemenu)

buttonspeed.Size = UDim2.new(0, 100,0, 33)

buttonspeed.BackgroundTransparency = 0.4

buttonspeed.Position = UDim2.new(0.800, 0,0.305, 0)

buttonspeed.Text = "ON"

buttonspeed.TextScaled = true



local bindspeed = Instance.new("TextButton", framemenu)

bindspeed.Size = UDim2.new(0, 100,0, 33)

bindspeed.BackgroundTransparency = 0.4

bindspeed.Position = UDim2.new(0.600, 0,0.305, 0)

bindspeed.Text = "[]"





local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")

local player = Players.LocalPlayer

local bindKey = Enum.KeyCode.X
local waitingForBind = false

local speedEnabled = false
local normalSpeed = 16
local speedValue = 50

bindspeed.Text = "" .. bindKey.Name

bindspeed.MouseButton1Click:Connect(function()
	waitingForBind = true
	bindspeed.Text = "Нажми клавишу"
end)

local function setSpeed(value)
	local character = player.Character

	if character then
		local humanoid = character:FindFirstChild("Humanoid")

		if humanoid then
			humanoid.WalkSpeed = value
		end
	end
end

UIS.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then return end

	if waitingForBind then
		if input.KeyCode ~= Enum.KeyCode.Unknown then
			bindKey = input.KeyCode
			button.Text = "" .. bindKey.Name
			waitingForBind = false
		end

		return
	end

	if input.KeyCode == bindKey then
		speedEnabled = not speedEnabled

		if speedEnabled then
			setSpeed(speedValue)
		else
			setSpeed(normalSpeed)
		end
	end
end)







local player = game:GetService("Players").LocalPlayer

local normalSpeed = 16
local sprintSpeed = 50

local enabled = false

buttonspeed.MouseButton1Click:Connect(function()
	enabled = not enabled

	local character = player.Character or player.CharacterAdded:Wait()
	local humanoid = character:WaitForChild("Humanoid")

	if enabled then
		humanoid.WalkSpeed = sprintSpeed
		buttonspeed.Text = "Off Speed"
	else
		humanoid.WalkSpeed = normalSpeed
		buttonspeed.Text = "Speed"
	end
end)

local textlabelinfjump = Instance.new("TextLabel", framemenu)

textlabelinfjump.Size = UDim2.new(0, 558,0, 48)

textlabelinfjump.BackgroundTransparency = 0.4

textlabelinfjump.Position = UDim2.new(0, 0,0.42, 0)

textlabelinfjump.Text = "InfJump - Бесконечный прыжок                                     "

textlabelinfjump.TextSize = 15

local buttoninfjump = Instance.new("TextButton", framemenu)

buttoninfjump.Size = UDim2.new(0, 100,0, 33)

buttoninfjump.BackgroundTransparency = 0.4

buttoninfjump.Position = UDim2.new(0.800, 0,0.436, 0)


buttoninfjump.Text = "ON"

buttoninfjump.TextScaled = true


local bindinfjump = Instance.new("TextButton", framemenu)

bindinfjump.Size = UDim2.new(0, 100,0, 33)

bindinfjump.BackgroundTransparency = 0.4

bindinfjump.Position = UDim2.new(0.600, 0,0.436, 0)

bindinfjump.Text = "[]"







local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")

local player = Players.LocalPlayer

local bindKey = Enum.KeyCode.J
local waitingForBind = false

local infJump = false

bindinfjump.Text = "" .. bindKey.Name

bindinfjump.MouseButton1Click:Connect(function()
	waitingForBind = true
	bindinfjump.Text = "Нажми клавишу"
end)

UIS.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then return end

	if waitingForBind then
		if input.KeyCode ~= Enum.KeyCode.Unknown then
			bindKey = input.KeyCode
			bindinfjump.Text = "" .. bindKey.Name
			waitingForBind = false
		end

		return
	end

	if input.KeyCode == bindKey then
		infJump = not infJump
	end

	if infJump and input.KeyCode == Enum.KeyCode.Space then
		local character = player.Character

		if character then
			local humanoid = character:FindFirstChildOfClass("Humanoid")

			if humanoid then
				humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
			end
		end
	end
end)









local player = game:GetService("Players").LocalPlayer

local infiniteJump = false
local jumpConnection

buttoninfjump.MouseButton1Click:Connect(function()
	infiniteJump = not infiniteJump

	if infiniteJump then
		buttoninfjump.Text = "Off InfJump"

		jumpConnection = game:GetService("UserInputService").JumpRequest:Connect(function()
			local character = player.Character or player.CharacterAdded:Wait()
			local humanoid = character:FindFirstChildOfClass("Humanoid")

			if humanoid then
				humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
			end
		end)

	else
		buttoninfjump.Text = "InfJump"

		if jumpConnection then
			jumpConnection:Disconnect()
			jumpConnection = nil
		end
	end
end)

local textlabelcarfly = Instance.new("TextLabel", framemenu)

textlabelcarfly.Size = UDim2.new(0, 558,0, 48)

textlabelcarfly.Position = UDim2.new(0, 0,0.55, 0)

textlabelcarfly.BackgroundTransparency = 0.4

textlabelcarfly.Text = "Car Fly - Летающий автомобиль                                   "

textlabelcarfly.TextSize = 15

local buttoncarfly = Instance.new("TextButton", framemenu)

buttoncarfly.Size = UDim2.new(0, 100,0, 33)

buttoncarfly.BackgroundTransparency = 0.4

buttoncarfly.Position = UDim2.new(0.800, 0,0.570, 0)

buttoncarfly.Text = "ON"

buttoncarfly.TextScaled = true




local bindcarfly = Instance.new("TextButton", framemenu)

bindcarfly.Size = UDim2.new(0, 100,0, 33)

bindcarfly.BackgroundTransparency = 0.4

bindcarfly.Position = UDim2.new(0.600, 0,0.570, 0)

bindcarfly.Text = "[]"

















local player = game:GetService("Players").LocalPlayer

local flying = false
local speed = 600

local bodyVelocity
local bodyGyro

buttoncarfly.MouseButton1Click:Connect(function()
	flying = not flying

	local character = player.Character or player.CharacterAdded:Wait()
	local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

	if flying then
		buttoncarfly.Text = "OFF"

		bodyVelocity = Instance.new("BodyVelocity")
		bodyVelocity.MaxForce = Vector3.new(999999,999999,999999)
		bodyVelocity.Velocity = Vector3.zero
		bodyVelocity.Parent = humanoidRootPart

		bodyGyro = Instance.new("BodyGyro")
		bodyGyro.MaxTorque = Vector3.new(999999,999999,999999)
		bodyGyro.CFrame = humanoidRootPart.CFrame
		bodyGyro.Parent = humanoidRootPart

		game:GetService("RunService").RenderStepped:Connect(function()
			if flying and bodyVelocity and bodyGyro then
				local camera = workspace.CurrentCamera
				bodyGyro.CFrame = camera.CFrame

				local moveDirection = Vector3.zero

				if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.W) then
					moveDirection += camera.CFrame.LookVector
				end
				if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.S) then
					moveDirection -= camera.CFrame.LookVector
				end
				if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.A) then
					moveDirection -= camera.CFrame.RightVector
				end
				if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.D) then
					moveDirection += camera.CFrame.RightVector
				end

				bodyVelocity.Velocity = moveDirection * speed
			end
		end)

	else
		buttoncarfly.Text = "ON"

		if bodyVelocity then
			bodyVelocity:Destroy()
			bodyVelocity = nil
		end

		if bodyGyro then
			bodyGyro:Destroy()
			bodyGyro = nil
		end
	end
end)









local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

local bindKey = Enum.KeyCode.C
local waitingForBind = false

local flying = false
local speed = 600

local bodyVelocity
local bodyGyro
local flyConnection

bindcarfly.Text = "" .. bindKey.Name

-- настройка бинда
bindcarfly.MouseButton1Click:Connect(function()
	waitingForBind = true
	bindcarfly.Text = "Нажми клавишу"
end)

local function startFly()
	local character = player.Character or player.CharacterAdded:Wait()
	local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

	bodyVelocity = Instance.new("BodyVelocity")
	bodyVelocity.MaxForce = Vector3.new(999999,999999,999999)
	bodyVelocity.Velocity = Vector3.zero
	bodyVelocity.Parent = humanoidRootPart

	bodyGyro = Instance.new("BodyGyro")
	bodyGyro.MaxTorque = Vector3.new(999999,999999,999999)
	bodyGyro.CFrame = humanoidRootPart.CFrame
	bodyGyro.Parent = humanoidRootPart

	flyConnection = RunService.RenderStepped:Connect(function()
		if flying and bodyVelocity and bodyGyro then
			local camera = workspace.CurrentCamera
			bodyGyro.CFrame = camera.CFrame

			local moveDirection = Vector3.zero

			if UIS:IsKeyDown(Enum.KeyCode.W) then
				moveDirection += camera.CFrame.LookVector
			end

			if UIS:IsKeyDown(Enum.KeyCode.S) then
				moveDirection -= camera.CFrame.LookVector
			end

			if UIS:IsKeyDown(Enum.KeyCode.A) then
				moveDirection -= camera.CFrame.RightVector
			end

			if UIS:IsKeyDown(Enum.KeyCode.D) then
				moveDirection += camera.CFrame.RightVector
			end

			bodyVelocity.Velocity = moveDirection * speed
		end
	end)
end

local function stopFly()
	if flyConnection then
		flyConnection:Disconnect()
	end

	if bodyVelocity then
		bodyVelocity:Destroy()
		bodyVelocity = nil
	end

	if bodyGyro then
		bodyGyro:Destroy()
		bodyGyro = nil
	end
end

UIS.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then return end

	-- выбор клавиши
	if waitingForBind then
		if input.KeyCode ~= Enum.KeyCode.Unknown then
			bindKey = input.KeyCode
			button.Text = "" .. bindKey.Name
			waitingForBind = false
		end

		return
	end

	-- включение carfly
	if input.KeyCode == bindKey then
		flying = not flying

		if flying then
			startFly()
		else
			stopFly()
		end
	end
end)










local textlabelesp = Instance.new("TextLabel", framemenu)

textlabelesp.Size = UDim2.new(0, 558,0, 50)

textlabelesp.Position = UDim2.new(0, 0,0.69, 0)

textlabelesp.Text = "                            позволяет видеть игроков"

textlabelesp.TextSize = 17

local buttonesp = Instance.new("TextButton", framemenu)

buttonesp.Size = UDim2.new(0, 150,0, 35)

buttonesp.Position = UDim2.new(0.027, 0,0.710, 0)

buttonesp.Text = "Esp"

buttonesp.TextScaled = true





local Players = game:GetService("Players")

local LocalPlayer = Players.LocalPlayer
local ESPEnabled = false
local highlights = {}

local function createESP(player)
	if player == LocalPlayer then return end

	local function setup(character)
		if highlights[player] then
			highlights[player]:Destroy()
		end

		local highlight = Instance.new("Highlight")
		highlight.Name = "ESP"
		highlight.Adornee = character
		highlight.FillColor = Color3.fromRGB(255, 0, 0)
		highlight.FillTransparency = 0.5
		highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
		highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
		highlight.Enabled = ESPEnabled
		highlight.Parent = character

		highlights[player] = highlight
	end

	if player.Character then
		setup(player.Character)
	end

	player.CharacterAdded:Connect(setup)
end

for _, player in pairs(Players:GetPlayers()) do
	createESP(player)
end

Players.PlayerAdded:Connect(createESP)

buttonesp.MouseButton1Click:Connect(function()
	ESPEnabled = not ESPEnabled

	for _, highlight in pairs(highlights) do
		if highlight then
			highlight.Enabled = ESPEnabled
		end
	end

	buttonesp.Text = ESPEnabled and "Off Esp" or "Esp"
end)




local textlabelaimbot = Instance.new("TextLabel", framemenu)

textlabelaimbot.Size = UDim2.new(0, 558,0, 50)

textlabelaimbot.Position = UDim2.new(0, 0,0.83, 0)

textlabelaimbot.Text = "                            позволяет целиться на игроков"

textlabelaimbot.TextSize = 17

local buttonaimbot = Instance.new("TextButton", framemenu)

buttonaimbot.Size = UDim2.new(0, 150,0, 35)

buttonaimbot.Position = UDim2.new(0.027, 0,0.847, 0)

buttonaimbot.Text = "Aimbot"

buttonaimbot.TextScaled = true





local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local AimbotEnabled = false
local Holding = false

-- Проверка игрока
local function isValidPlayer(player)
	if player == LocalPlayer then
		return false
	end

	local character = player.Character
	if not character then
		return false
	end

	local humanoid = character:FindFirstChild("Humanoid")
	local head = character:FindFirstChild("Head")

	return humanoid and humanoid.Health > 0 and head
end

-- Ближайший игрок
local function getClosestPlayer()
	local closest = nil
	local shortest = math.huge

	for _, player in pairs(Players:GetPlayers()) do
		if isValidPlayer(player) then
			local head = player.Character.Head

			local pos, visible = Camera:WorldToViewportPoint(head.Position)

			if visible then
				local distance = (
					Vector2.new(pos.X, pos.Y) -
						Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
				).Magnitude

				if distance < shortest then
					shortest = distance
					closest = player
				end
			end
		end
	end

	return closest
end

-- Кнопка GUI
buttonaimbot.MouseButton1Click:Connect(function()
	AimbotEnabled = not AimbotEnabled

	buttonaimbot.Text = AimbotEnabled and "Off Aimbot" or "Aimbot"
end)

-- ПКМ
UserInputService.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then return end

	if input.UserInputType == Enum.UserInputType.MouseButton2 then
		Holding = true
	end
end)

UserInputService.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton2 then
		Holding = false
	end
end)

-- Наводка
RunService.RenderStepped:Connect(function()
	if AimbotEnabled and Holding then
		local target = getClosestPlayer()

		if target and target.Character and target.Character:FindFirstChild("Head") then
			Camera.CFrame = CFrame.new(
				Camera.CFrame.Position,
				target.Character.Head.Position
			)
		end
	end
end)







local UIS = game:GetService("UserInputService")

UIS.InputBegan:Connect(function(input, gameProcessed)
	if gameProcessed then return end

	if input.KeyCode == Enum.KeyCode.T then
		framemenu.Visible = not framemenu.Visible
	end
end)





